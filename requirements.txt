# Core ingestion
boto3>=1.34.0
requests>=2.31.0
pydantic>=2.6.0
tenacity>=8.2.0

# Orchestration
dagster>=1.7.0
dagster-webserver>=1.7.0

# Dev / testing
pytest>=8.0.0
moto[s3]>=5.0.0          # Mock AWS for tests
responses>=0.25.0         # Mock HTTP for tests
ruff>=0.4.0              # Lint + format
mypy>=1.10.0
requests>=2.31.0
pyarrow>=15.0.0